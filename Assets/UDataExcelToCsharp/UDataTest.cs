using System;
using System.Collections.Generic;
using s7u.dtb.exceldata;
using uData;
using UnityEngine;

public class UDataTest : MonoBehaviour
{
    public TextAsset mTextAsset;
    // Start is called before the first frame update
    void Start()
    {
        // DataTableService mManager = new DataTableService();
        // DataTableService.DataTable<LevelData> mLeveDataTable = mManager.LoadDataTable<LevelData>(mTextAsset.text);
        // foreach (var v in mLeveDataTable.GetAllDatas())
        // {
        //     LevelData data = v;
        //     Debug.Log(data.UnlockingLevelID[0] + ", " + data.UnlockingLevelID[1]);
        // }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
